interface Book extends Product
{
    String getTitle();

    static Book createBook(String title)
    {
        Product product = Product.extendProduct();
        return new BookImpl(product, title);
    }

    static Book extendBook(Product product, String title)
    {
        return new BookImpl(product, title);
    }
}