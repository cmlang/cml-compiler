interface Book
{
    String getTitle();

    Optional<Book> getSequel();

    List<Category> getCategories();

    static Book createBook(String title, @Nullable Book sequel, List<Category> categories)
    {
        return new BookImpl(title, sequel, categories);
    }

    static Book extendBook(String title, @Nullable Book sequel, List<Category> categories)
    {
        return new BookImpl(title, sequel, categories);
    }
}