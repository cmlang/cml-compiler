interface Book extends Intermediate
{
    static Book createBook(String description)
    {
        Product product = Product.extendProduct(description);
        Intermediate intermediate = Intermediate.extendIntermediate(product);
        return new BookImpl(product, intermediate);
    }

    static Book extendBook(Product product, Intermediate intermediate)
    {
        return new BookImpl(product, intermediate);
    }
}