interface Book extends Product
{
    String getTitle();

    static Book createBook(String title)
    {
        Product product = Product.createProductImpl();
        return new BookImpl(product, title);
    }

    static Book createBookImpl(Product product, String title)
    {
        return new BookImpl(product, title);
    }
}