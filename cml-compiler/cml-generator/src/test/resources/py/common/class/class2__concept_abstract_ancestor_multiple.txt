class BookImpl(ABC, Book):

    def __init__(self, base: Base, product: Product, stock_item: StockItem, title: str):
        self.__base = base
        self.__product = product
        self.__stock_item = stock_item
        self.__title = title

    @property
    def title(self) -> str:
        return self.__title

    def base_property(self) -> str:
        return self.__base.base_property

    def description(self) -> str:
        return self.__product.description

    def quantity(self) -> int:
        return self.__stock_item.quantity

    def __str__(self) -> str:
        return "%s(base_property=%s, description=%s, quantity=%s, title=%s)" % (
            type(self).__name__,
            self.base_property,
            self.description,
            self.quantity,
            self.title
        )